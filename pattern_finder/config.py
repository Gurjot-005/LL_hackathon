# config.py

# Path to dataset (relative to pattern_finder/)
DATA_PATH = "../data/raw"

# Correlation threshold for linking cells
CORRELATION_THRESHOLD = 0.7

# Rolling window size (not used yet, reserved for smoothing later)
ROLLING_WINDOW = 10

# Output folder
OUTPUT_DIR = "outputs"
